<!-- normal -->
# New Markdown to HTML Converter

## Normal Paragraph Spacing

This is a sample paragraph that uses the `_normal_` paragraph spacing. This is a sample paragraph that uses the `_normal_` paragraph spacing. This is a sample paragraph that uses the `_normal_` paragraph spacing. This is a sample paragraph that uses the `_normal_` paragraph spacing. This is a sample paragraph that uses the `_normal_` paragraph spacing.

* Here is bullet number 1.
* Bullet number 2
* etc.

<!-- compact -->

## Compact Paragraph Spacing

This is a sample paragraph that uses the `_compact_` paragraph spacing. This is a sample paragraph that uses the `_compact__` paragraph spacing. This is a sample paragraph that uses the `_compact_` paragraph spacing. This is a sample paragraph that uses the `_compact_` paragraph spacing. This is a sample paragraph that uses the `_compact_` paragraph spacing.

* Here is bullet number 1.
* Bullet number 2
* etc.